set(x /usr/local/bin)
set(x a/b/c.d)
set(x -DFOO=bar)
set(x --option=value)
set(x $ENV{PATH})
set(x ${CMAKE_SOURCE_DIR}/foo)
set(x "$<TARGET_FILE:tgt>")
set(x a${b}c${d}e)
if(x MATCHES "^[a-z]+$")
endif()
