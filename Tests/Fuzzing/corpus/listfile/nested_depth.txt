if(TRUE)
  if(TRUE)
    if(TRUE)
      foreach(i RANGE 1)
        function(f${i})
          message("deep")
        endfunction()
        f0()
      endforeach()
    endif()
  endif()
endif()
macro(outer)
  function(inner)
    block()
      while(FALSE)
      endwhile()
    endblock()
  endfunction()
  inner()
endmacro()
