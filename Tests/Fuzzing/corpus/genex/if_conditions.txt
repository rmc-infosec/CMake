$<IF:1,true_val,false_val>
$<IF:0,true_val,false_val>
$<IF:$<BOOL:YES>,enabled,disabled>
$<IF:$<BOOL:NO>,enabled,disabled>
$<IF:$<BOOL:ON>,on,off>
$<IF:$<BOOL:OFF>,on,off>
$<IF:$<BOOL:TRUE>,t,f>
$<IF:$<BOOL:FALSE>,t,f>
$<IF:$<BOOL:>,empty,notempty>
$<IF:$<BOOL:NOTFOUND>,found,notfound>
$<IF:$<BOOL:FOO-NOTFOUND>,found,notfound>
$<IF:$<AND:1,1>,both,not>
$<IF:$<AND:1,0>,both,not>
$<IF:$<OR:0,1>,either,neither>
$<IF:$<OR:0,0>,either,neither>
$<IF:$<NOT:0>,negated,orig>
$<IF:$<NOT:1>,negated,orig>
$<IF:$<STREQUAL:abc,abc>,same,diff>
$<IF:$<STREQUAL:abc,def>,same,diff>
$<IF:$<EQUAL:42,42>,same,diff>
$<IF:$<EQUAL:1,2>,same,diff>
$<IF:$<IN_LIST:foo,a;b;foo;c>,found,missing>
$<IF:$<IN_LIST:bar,a;b;foo;c>,found,missing>
$<IF:$<VERSION_GREATER:3.0,2.0>,newer,older>
$<IF:$<VERSION_LESS:1.0,2.0>,older,newer>
