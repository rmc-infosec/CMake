{
  "$comment": [
    "example comment",
    "with an array of strings"
  ],
  "version": 10,
  "configurePresets": [
    {
      "name": "CommentValid",
      "generator": "x",
      "binaryDir": "${sourceDir}/build"
    }
  ]
}
